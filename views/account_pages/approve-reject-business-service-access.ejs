<% pageTitle = "Approve or reject premium application - Get a document legalised – GOV.UK" %>

<%- include('../partials/header.ejs') %>

<% if(requestType === 'approve') { %>
  <h1 class="heading-xlarge">Approve Next-Day service access</h1>
<% } else { %>
    <h1 class="heading-xlarge">Reject Next-Day service access</h1>
  <% } %>

  <% if (success === false) { %>

    <p>We could not find a user account associated with this request.</p>

  <% } else { %>

      <% if(requestType === 'approve') { %>

        <p>Next-Day service access has been granted for <%- userEmail %>.</p>

      <% } else { %>

          <p>Next-Day service access has been rejected for <%- userEmail %>.</p>

        <% } %>

      <% } %>

      <button type="submit" class="govuk-button" onclick="window.open('', '_self', ''); window.close();">Close window</button>

      <%- include('../partials/footer.ejs') %>
